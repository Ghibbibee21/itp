number = int(input("Oy mate, how big do ya want this pyramid? "))
if number < 1 or number > 8:
    print ("no")
else: number >= 1 and number <= 8
for i in range(number + 1, 0, -1):
  for j in range(number):
    if j >= i:
      print('#', end='')
  print()